apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "secret-template-example.fullname" . }}-test-connection"
  labels:
    {{- include "secret-template-example.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "secret-template-example.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
