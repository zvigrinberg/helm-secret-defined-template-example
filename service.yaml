apiVersion: v1
kind: Service
metadata:
  name: {{ include "secret-template-example.fullname" . }}
  labels:
    {{- include "secret-template-example.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "secret-template-example.selectorLabels" . | nindent 4 }}
